# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1        # [standard] includes websocket & watchfiles support
python-multipart==0.0.9          # required by FastAPI for file uploads

# ── HTTP Client (Ollama communication) ────────────────────────────────────────
httpx==0.27.0                    # async HTTP client used for all Ollama API calls

# ── Vector Store ──────────────────────────────────────────────────────────────
chromadb==0.4.24                 # ChromaDB persistent vector store (0.4.x is stable LTS)

# ── Embeddings (local, no API key needed) ────────────────────────────────────
sentence-transformers==2.7.0     # all-MiniLM-L6-v2 local embedding model
torch==2.10.0                     # required by sentence-transformers
# NOTE: for CPU-only: pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cpu

# ── Document Parsing ──────────────────────────────────────────────────────────
pypdf==4.2.0                     # PDF text extraction
python-docx==1.1.2               # DOCX text extraction

# ── Explainability ────────────────────────────────────────────────────────────
shap==0.45.1                     # SHAP Shapley value explanations
scikit-learn           		 # TF-IDF, LogisticRegression, cosine_similarity
numpy                 		 # numerical operations for SHAP/sklearn

# ── Data & Config ─────────────────────────────────────────────────────────────
pandas                		# used by SHAP internally
pydantic                  	# FastAPI request/response validation
python-dotenv             	# .env config loading
